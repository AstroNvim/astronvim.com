---
import heroImage from "~/assets/space-timelapse.jpg";

const sizes = "100vw";
---

<img
  class="h-full w-full object-cover"
  src={heroImage.src}
  sizes={sizes}
  alt="The ridged surface of the moon"
/>
